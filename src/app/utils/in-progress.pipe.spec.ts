import { InProgressPipe } from './in-progress.pipe';

describe('InProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new InProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
